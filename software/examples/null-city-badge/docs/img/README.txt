Save your two badge photos here with these exact filenames:

  board-front.jpg  —  the photo showing the front face (display + buttons)
  board-back.jpg   —  the photo showing the back (ESP32, battery, USB-C)

The board-explorer.html file references these paths.
Any common image format works (JPG, PNG, WEBP) — just update the
src= lines in board-explorer.html if you use a different extension.
